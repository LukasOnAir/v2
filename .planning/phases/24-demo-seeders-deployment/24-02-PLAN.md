---
phase: 24-demo-seeders-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tester/TesterControlCard.tsx
  - src/pages/TesterDashboardPage.tsx
autonomous: true

must_haves:
  truths:
    - "TesterControlCard displays correctly on mobile viewport (320px-768px)"
    - "TesterDashboardPage stat cards stack vertically on mobile"
    - "Quick info row stacks vertically on mobile, displays inline on larger screens"
    - "Touch targets are at least 44px for interactive elements"
    - "Text remains readable without horizontal scrolling on mobile"
  artifacts:
    - path: "src/components/tester/TesterControlCard.tsx"
      provides: "Mobile-responsive control card for testers"
      contains: "flex-col sm:flex-row"
    - path: "src/pages/TesterDashboardPage.tsx"
      provides: "Mobile-responsive tester dashboard"
      contains: "grid-cols-1 sm:grid-cols-2"
  key_links:
    - from: "TesterControlCard.tsx"
      to: "Tailwind breakpoints"
      via: "sm: and lg: prefixes"
      pattern: "sm:|lg:|md:"
---

<objective>
Make the Control Tester interface mobile-responsive for field use.

Purpose: Control Testers often perform tests in the field (factory floor, branch offices, etc.) using mobile devices. The interface must be usable on small screens without horizontal scrolling or cramped touch targets.

Output: Updated TesterControlCard.tsx and TesterDashboardPage.tsx with mobile-first responsive styling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-demo-seeders-deployment/24-RESEARCH.md

# Files to modify
@src/components/tester/TesterControlCard.tsx
@src/pages/TesterDashboardPage.tsx

# Existing responsive pattern example
# TesterDashboardPage already has: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TesterControlCard for mobile</name>
  <files>src/components/tester/TesterControlCard.tsx</files>
  <action>
Update TesterControlCard.tsx to be mobile-responsive. Apply Tailwind's mobile-first approach: unprefixed classes for mobile, sm: for 640px+, lg: for 1024px+.

Key changes:

1. **Header section (lines 53-91):**
   - Change `flex items-start justify-between gap-4` to stack on mobile:
     `flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 sm:gap-4`
   - Status indicators should wrap below name on mobile

2. **Control name and badges:**
   - Name and badges currently inline - keep them wrapping naturally with `flex-wrap`
   - Badges: `flex flex-wrap items-center gap-2` (already wraps)

3. **Quick info row (lines 94-113):**
   - Currently: `flex items-center gap-6`
   - Change to: `flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-6`
   - Each info item stacks vertically on mobile, inline on larger screens

4. **Padding adjustments:**
   - Card padding: Change `p-4` to `p-3 sm:p-4`
   - Inner sections: Reduce spacing on mobile

5. **Touch targets:**
   - "Show Details" button: Ensure min-height of 44px
   - "Record Test Result" button: Add `min-h-[44px]` or ensure padding provides it

6. **Text sizing:**
   - Control name: `text-sm sm:text-base` (smaller on mobile)
   - Quick info text: `text-xs sm:text-sm`

7. **Expanded content section:**
   - Reduce spacing in expanded section on mobile
   - Test history items: Stack date below result on mobile
  </action>
  <verify>
1. Open browser, navigate to Tester Dashboard (in demo mode)
2. Use browser DevTools to test at 320px, 375px, 414px widths
3. Verify:
   - No horizontal scroll
   - All text readable
   - Buttons tappable (visually check size)
   - Information hierarchy maintained
  </verify>
  <done>TesterControlCard displays correctly on mobile viewports (320px-768px)</done>
</task>

<task type="auto">
  <name>Task 2: Update TesterDashboardPage for mobile</name>
  <files>src/pages/TesterDashboardPage.tsx</files>
  <action>
Update TesterDashboardPage.tsx for improved mobile experience.

Key changes:

1. **Header section (lines 45-50):**
   - Add responsive padding: `p-4 sm:p-6`
   - Title size: `text-xl sm:text-2xl`

2. **Stats grid (lines 53-82):**
   - Already has `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` - good!
   - Update grid padding: `p-4 sm:p-6`
   - Gap: `gap-3 sm:gap-4`

3. **Section headers (Overdue, Due This Week, Up to Date):**
   - Reduce top margin on mobile: `mt-4 sm:mt-6`
   - Font size: `text-xs sm:text-sm` for section titles

4. **Controls list container (line 85):**
   - Padding: `p-4 sm:p-6 pt-0`
   - Spacing between cards: `space-y-3 sm:space-y-4`

5. **StatCard component (lines 149-161):**
   - Padding: `p-3 sm:p-4`
   - Value text: `text-xl sm:text-2xl`
   - Icon size: responsive via CSS or keep as-is (8x8 is fine)

6. **EmptyState component (lines 163-176):**
   - Padding: `py-8 sm:py-12`
   - Icon: `w-12 h-12 sm:w-16 sm:h-16`
   - Max width on message: `max-w-sm sm:max-w-md`
  </action>
  <verify>
1. Open browser, navigate to Tester Dashboard
2. Test at 320px, 375px, 414px, 768px widths
3. Verify:
   - Stat cards stack correctly (1 col mobile, 2 cols tablet, 4 cols desktop)
   - Section headers readable
   - Controls list scrollable
   - Empty state looks good on all sizes
  </verify>
  <done>TesterDashboardPage displays correctly on mobile viewports</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Start dev server: `npm run dev`
2. Navigate to Tester Dashboard (select Control Tester role in demo mode)
3. Test responsive behavior at key breakpoints:
   - 320px (small phone)
   - 375px (iPhone SE/8)
   - 414px (iPhone Plus/Max)
   - 768px (tablet)
   - 1024px+ (desktop)

4. For each breakpoint, verify:
   - No horizontal scrolling required
   - All content visible and readable
   - Buttons large enough for touch (44px min)
   - Cards stack/flow appropriately
   - Information hierarchy preserved
</verification>

<success_criteria>
- TesterControlCard and TesterDashboardPage render correctly at 320px width
- No horizontal overflow on mobile
- Touch targets are 44px minimum height
- Text is readable without zooming
- Responsive classes follow mobile-first pattern (unprefixed for mobile, sm: for larger)
</success_criteria>

<output>
After completion, create `.planning/phases/24-demo-seeders-deployment/24-02-SUMMARY.md`
</output>
